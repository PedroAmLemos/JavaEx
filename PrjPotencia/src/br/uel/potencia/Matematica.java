package br.uel.potencia;

public class Matematica {
	public static double pot(double a, double b) {
		return Math.pow(a, b);
	}
	public static int min(double a, double b) {
		return Math.min((int) a, (int) b);
	}
}
